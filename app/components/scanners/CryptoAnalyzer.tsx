'use client';

import { Card, CardContent, CardHeader, CardTitle } from '../components/ui/card';
import React from 'react';
import {  Card ,  Card, CardHeader, CardContent, CardTitle  } from '../components/ui/card';

export default function CryptoAnalyzer() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Crypto Analyzer</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Component functionality coming soon.</p>
      </CardContent>
    </Card>
  );
}
